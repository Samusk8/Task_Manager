


const categoryInput = document.getElementById("category");
const categoryColor = document.getElementById("color");

function addCategory() {
    const name = categoryInput.value;
    const color = categoryColor.value;
}